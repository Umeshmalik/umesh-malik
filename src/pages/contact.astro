---
import Desktop from "../layouts/Desktop.astro";
import DesktopApp from "../components/DesktopApp";
import DraggableWindow from "../components/windows/DraggableWindow";
import ContactContent from "../components/pages/ContactContent";
---

<Desktop
  title="mail.exe - Contact | umesh.OS"
  description="Get in touch with Umesh Malik - Senior Frontend Engineer. Email, LinkedIn, and GitHub."
  currentPage="contact"
>
  <DesktopApp currentPage="contact" client:load>
    <DraggableWindow
      title="mail.exe - umesh.OS Mail"
      icon="📧"
      defaultWidth={700}
      defaultHeight={500}
      closeHref="/"
      client:load
    >
      <ContactContent client:load />
    </DraggableWindow>
  </DesktopApp>
</Desktop>
